Which pokemon files do you wish to include? Enter a number (1-9), F to finish entering files, or E to exit the program:
if 1:
	vector<int> chosenFiles.push_back(1);
etc
while not finished:
	//allow user to enter more files

for int (i = 0; i < chosenFiles.size(); ++i)
{
	catalogue.read(Pokemon'i'.txt); //if the user has added 2, then Pokemon2.txt should be read into the program
}

menu:
Enter A to see all Pokemon, G to sort by generation, 
